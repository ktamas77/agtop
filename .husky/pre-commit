echo "▶ deno fmt --check"
deno fmt --check || exit 1

echo "▶ deno lint"
deno lint || exit 1

echo "▶ deno check + test"
deno check main.ts || exit 1
deno test -A || exit 1

echo "▶ tsc build (npm dist)"
npm run build || exit 1
